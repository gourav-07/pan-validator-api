# PAN Card Validator API (Spring Boot)  
A REST API to validate Indian PAN card numbers via regex and govt database checks.  

## Features  
- Regex validation (`ABCDE1234F` format).  
- Scraping govt portals (if allowed).  

## How to Run  
```bash
mvn spring-boot:run
